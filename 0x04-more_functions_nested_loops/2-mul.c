#include "main.h"

/**
 * mul - multiplies two integers
 * @x: int to be multiplied to b
 * @y: int to be multiplied to a
 * Return: the result of the operation
 */
int mul(int x, int y)
{
int z;

z = x * y;

return (z);
}
